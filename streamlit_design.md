# Streamlitアプリのデザイン指示書

既存の最適化計算プログラムをモジュールとして用いて、ユーザーフレンドリーなGUIアプリを作成する。

## 画面構成

- **サイドバー**
  - CSVファイル読み込み（カード一覧CSVのみ、任意）
  - 「ファイル読み込み」ボタンでフォームに反映
- **メインエリア**
  - タイトル・説明文
  - アイテム数入力
  - カード数指定
  - カード入力フォーム
  - 入力データのダウンロードボタン
  - 最適化実行ボタン
  - 最適化結果表示

## サイドバー

- `st.sidebar.file_uploader` を用いて **カード一覧CSV** をアップロード
  - フォーマット: `name, rare, mas, skill, wl`
- アップロード後、`st.sidebar.button("ファイル読み込み")` で
  フォーム入力欄に反映

## メインエリア

### 1. タイトル・説明

- `st.title`, `st.caption` でアプリ名と簡易説明を表示

### 2. アイテム数入力

- 4列レイアウトでそれぞれ
  - `想いのカケラ`
  - `想いの純結晶`
  - `スキルスコア中級`
  - `スキルスコア上級`
  を `st.number_input` で入力（最小 0）

### 3. カード数指定

- `st.number_input` で入力欄数を指定（最小 20, 最大 100）

### 4. カード入力フォーム

- 指定枚数分の `st.expander` 内に5列レイアウト
  - `name`: `st.text_input`（空欄許可、空欄時は自動で `Card [番号]`）
  - `rare`: `st.selectbox`（`["☆4", "BD/AN"]`）
  - `mas`: `st.number_input`（0〜5）
  - `skill`: `st.number_input`（1〜4）
  - `wl`: `st.checkbox`

### 5. 入力データのダウンロード

- `st.download_button` でカード一覧をCSV出力
  - ファイル名: `card_list.csv`

### 6. 最適化実行

- `st.button` で最適化開始

### 7. 最適化結果表示

- サブヘッダー `最適化結果`
- 3列レイアウトで `st.metric`
  - 最終倍率
  - カケラ使用量
  - スキルスコア使用量（カケラ換算）
- サブヘッダー `強化後のサポートユニット`
- 説明文 `st.caption`（MR=マスターランク, SL=スキルレベル, コスト換算）
- `st.dataframe` で日本語化したカラムを指定順で表示
- `st.download_button` で結果CSVを出力（不要であれば削除可）
