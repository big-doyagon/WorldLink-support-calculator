# Streamlitアプリのデザイン指示書

既存の最適化計算プログラムをモジュールとして用いて、ユーザーフレンドリーなGUIアプリを作成する。

## 画面構成

大きく分けて左半分に入力、右半分に出力を表示する。

## モード遷移

st.tabsを用いて、以下の2つの画面を表示する。

1. CSVファイル読み込み
2. 直接入力

## CSVファイル読み込み

st.file_uploaderを用いて、カード一覧CSVをアップロードする。

## 直接入力

### カード一覧

- デフォルトでは20枚分のカード入力フォームを作成するが、それ以上にも対応できるようにする
- カード名はst.text_inputを用いる
- レアリティ、マスターランク、スキルレベルはst.selectboxを用いる
- WL限定はst.checkboxを用いる

### アイテム一覧

- st.number_inputを用いて、アイテム数を入力する

### 最適化実行

- st.buttonを用いて、最適化を実行する

## 出力

基本的には最適化結果を出力するだけだが、現状（強化前）のカード一覧とアイテム一覧をCSVとしてダウンロードできるボタンを用意する。これは、ユーザーが2回目以降の入力を容易にするためである。

逆に、結果のCSVのダウンロードは不要である。ユーザーは結果のCSVを眺めることはしないため。


